spring:
  application:
    name: cloud-config-server
  cloud:
      config:
        server:
          git:
            uri: https://github.com/kiranrchitragar/cloud-config-server

server:
  port: 8888
